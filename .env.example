# Environment Variables Configuration

# ===========================================
# DATABASE (Supabase PostgreSQL)
# ===========================================
# Get these from: Supabase Dashboard → Settings → Database
# Copy "Connection string" in URI mode

# Pooled connection (for application queries)
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection (for Prisma migrations)
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# ===========================================
# NEXTAUTH (Authentication)
# ===========================================
# For local development
NEXTAUTH_URL="http://localhost:3000"

# Generate a secure secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-change-this-in-production"

# ===========================================
# EMAIL SERVICE (Resend)
# ===========================================
# Sign up at https://resend.com to get your API key
RESEND_API_KEY="re_your_resend_api_key"
EMAIL_FROM="noreply@yourdomain.com"

# ===========================================
# APPLICATION
# ===========================================
APP_URL="http://localhost:3000"
APP_NAME="SmartNews Intelligence"

# ===========================================
# AI AGENT (Anthropic Claude)
# ===========================================
# Get your API key from: https://console.anthropic.com/
# Required for AI-powered news search feature
ANTHROPIC_API_KEY="sk-ant-api03-your-api-key-here"

# ===========================================
# NOTES
# ===========================================
# 1. Replace [PROJECT-REF], [YOUR-PASSWORD], and [REGION] with actual values from Supabase
# 2. For production (Vercel), set NEXTAUTH_URL to your actual domain
# 3. Never commit this file with real credentials - use .env.local instead
# 4. Supabase connection strings use port 6543 for pooled, 5432 for direct
